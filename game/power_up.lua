-- ACTIVATES A POWERUP DEPENDING ON pu-type
function activate_power_up(pu_name)
	if(pu_name=="powerup1") then

	elseif(pu_name=="powerup2") then
		game_score = game_score+10001
	elseif(pu_name=="powerup3") then

	elseif(pu_name=="powerup4") then

	elseif(pu_name=="powerup5") then

  	end
end